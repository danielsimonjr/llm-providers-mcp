# openai skill

Load id: `llm-openai:openai`. Slash trigger: `/openai`.

Routes questions to OpenAI via the `llm-openai` MCP server: `openai_quick_query` (gpt-4o-mini) for everyday questions, `openai_reasoning_query` (o3-mini) for architecture, hard bugs, and formal reasoning.

See `SKILL.md` for the full playbook — tool selection, answer format, and the text-only gotcha.
